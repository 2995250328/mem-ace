# 后续修改思路

## 用 ACE 原版 Backbone 替换 DINO（验证 LMC 通用性）

已写好**新对话用任务 Prompt**，可直接复制到新会话以驱动实现：  
→ **[docs/prompt/prompt_替换ACE原版backbone.md](prompt/prompt_替换ACE原版backbone.md)**  
其中约定了：角色与准则、任务目标、必须使用的 Skills（brainstorming → writing-plans / TDD → verification）、关键代码位置、接口契约与验收方式；要求**完全不影响**现有 DINO 逻辑。

---

## 采样逻辑

基于**原版 ACE** 先验证逻辑可行性：用训练好的 ACE 在（可加噪）原图上推理，以重投影误差小的点作为真值，**先单独 e2e 训练采样器**（不用 buffer），再用训练好的采样器**替换** buffer 收集时的随机采样，按高置信度选点。可选参考 `ace_trainer_full.py` 的 SuperPoint 邻域采样与 Part A/B 结构。**要求**：新代码放入专用子目录（如 `ace_depth/ace_sampler/`）。

已写好**新对话用任务 Prompt**（中英双语）：  
→ **[docs/prompt/prompt_sampling_logic.md](prompt/prompt_sampling_logic.md)**

## 用 SceneTok 替换现在的 LMC 模块

用预训练好的新视角合成方法 SceneTok 得到的场景非结构表示（压缩 scene tokens）作为全局 memory，与 query 特征进行融合，实现特征增强。**要求**：所有 ace-scenetok 相关新代码放入专用子目录（如 `ace_depth/ace_scenetok/`），不在主目录散落。

已写好**新对话用任务 Prompt**（中英双语）：  
→ **[docs/prompt/prompt_scenetok_replace_lmc.md](prompt/prompt_scenetok_replace_lmc.md)**

---

## 适配多场景训练
## 下载scannet，进行大规模训练
## 上采样特征
现有的backbone（DINO、ACE）都是一个下采样的结果，能否训练上采样模块（微调AnyUp），得到图像分辨率或较高分辨率的特征图，得到更细粒度的场景坐标回归结果
## 多分支训练
再添加一个APR分支联合训练，是否有效果？
## 更多信息的注入
自己在大规模场景中训练SceneTok的提取，可以添加上一些语言信息、显式的边缘图等，提升对场景结构、语义的表达能力
## 亟待优化探索的方向
特征融合部分仅仅是简单的cross attention，是否可根据这个任务做专门设计？
memory压缩一定要参考一下SceneTok，可以借鉴一下他的具体框架，再尝试一下优化，一定要摒弃掉现在的强依赖深度（具体的几何结构）的限制。