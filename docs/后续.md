# 后续修改思路
## 采样逻辑
用训练好的ACE model进行推理（这里指原版的ACE），可以尝试在原图上加噪声，看最后重投影误差小的点，作为真值，训练一个采样模块，让训练时能采样这些稳定且有意义的点。
## 用SceneTok替换现在的lmc模块
用预训练好的新视角合成方法得到的场景非结构表示，作为全局的memory与query特征进行融合，实现特征的增强。
## 适配多场景训练
## 下载scannet，进行大规模训练
## 上采样特征
现有的backbone（DINO、ACE）都是一个下采样的结果，能否训练上采样模块（微调AnyUp），得到图像分辨率或较高分辨率的特征图，得到更细粒度的场景坐标回归结果
## 多分支训练
再添加一个APR分支联合训练，是否有效果？
## 更多信息的注入
自己在大规模场景中训练SceneTok的提取，可以添加上一些语言信息、显式的边缘图等，提升对场景结构、语义的表达能力
## 亟待优化探索的方向
特征融合部分仅仅是简单的cross attention，是否可根据这个任务做专门设计？
memory压缩一定要参考一下SceneTok，可以借鉴一下他的具体框架，再尝试一下优化，一定要摒弃掉现在的强依赖深度（具体的几何结构）的限制。